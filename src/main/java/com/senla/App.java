package com.senla;


import java.io.IOException;

public class App {

    public static void main(String[] args) throws IOException, InterruptedException {
        /*ObjectMapper mapper = new ObjectMapper();

        String response = Geocoder.getGeocodeResponseFromCoordinates(55.730091, 37.639440);
        //String response = Geocoder.getGeocodeResponseFromQuery("Орёл, Подвесной мост");
        JsonNode responseJsonNode = mapper.readTree(response);

        JsonNode items = responseJsonNode.get("items");

        System.out.println(items);*/

        /*ConfigurableApplicationContext ctx = new AnnotationConfigApplicationContext(AppConfig.class);
        RentalPointService rentalPointService = ctx.getBean(RentalPointService.class);
        System.out.println(rentalPointService.getById(1));
        ctx.close();*/
    }
}
